import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
  button: {
    backgroundColor: '#007236',
    borderRadius: 10,
    width: '100%',
    padding: 10,
  },
  buttonText: {
    color: '#FFFFFF',
    fontWeight: '700',
    textAlign: 'center',
  },
  
});
